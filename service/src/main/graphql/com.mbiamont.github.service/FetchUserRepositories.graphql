query FetchUserPublicRepositories {
  viewer {
    avatarUrl
    repositories(first: 10) {
      totalCount
      nodes {
        name
        owner {
          avatarUrl
          login
        }
        stargazers {
          totalCount
        }
      }
    }
  }
}