query FetchRepositoryIssues($name: String!, $ownerLogin: String!, $since: DateTime!, $size: Int!, $afterCursor: String) {
    repository(name: $name, owner: $ownerLogin) {
        issues(after: $afterCursor, first: $size, filterBy: {since: $since}, orderBy: {field: CREATED_AT, direction: ASC}) {
            totalCount
            edges {
                cursor
                node {
                    state
                    title
                    createdAt
                    author {
                        avatarUrl
                        login
                    }
                    comments {
                        totalCount
                    }
                }
            }
            pageInfo {
                endCursor
                hasNextPage
            }
        }
    }
}
