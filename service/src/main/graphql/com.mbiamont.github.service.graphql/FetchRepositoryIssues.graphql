query FetchRepositoryIssues($name: String! $ownerLogin: String!) {
    repository(name: $name, owner: $ownerLogin) {
        issues(first: 10) {
            totalCount
            nodes {
                state
                title
                createdAt
            }
            pageInfo {
                endCursor
                hasNextPage
            }
        }
    }
}