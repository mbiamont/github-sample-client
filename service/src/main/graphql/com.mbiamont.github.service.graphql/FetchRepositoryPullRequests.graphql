query FetchRepositoryPullRequests($name: String!, $ownerLogin: String!, $size: Int!, $afterCursor: String) {
    repository(name: $name, owner: $ownerLogin) {
        pullRequests(after: $afterCursor, first: $size, orderBy: {field: CREATED_AT, direction: DESC}) {
            totalCount
            edges {
                cursor
                node {
                    id
                    author {
                        avatarUrl
                        login
                    }
                    comments {
                        totalCount
                    }
                    createdAt
                    title
                    state
                }
            }
            pageInfo {
                endCursor
                hasNextPage
            }
        }
    }
}